# 复制为 .env 后按需修改：cp .env.example .env

# 页面访问密码（留空则不启用密码保护）
APP_PASSWORD=changeme

# 会话 cookie 签名密钥，请改成随机字符串（如 openssl rand -hex 32）
SECRET_KEY=please-change-this-secret-key

# 下载并发线程数，默认 16
THREADS=16

# HTTP 代理（可选），例如 http://192.168.1.10:7890
PROXY=
